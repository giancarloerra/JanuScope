{
  "mcpServers": {
    "januscope": {
      "command": "npx",
      "args": ["-y", "januscope", "--config", "postgres-crystaldba"],
      "env": {
        "DATABASE_URI": "${DATABASE_URI}"
      }
    }
  }
}
